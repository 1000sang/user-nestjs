version: '3.2'
services:
  nest:
    build:
      context: ../
      dockerfile: ./docker/dockerfile
    ports:
      - '3000:3000'
    volumes:
      - ../src:/app/src
      - ../public:/app/pulic
      - ../identity:/app/identity
    env_file:
      - ../.env
